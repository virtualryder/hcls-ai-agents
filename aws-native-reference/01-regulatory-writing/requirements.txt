# Native rebuild — Lambdas + Strands drafting. Container runtime uses serve.py.
strands-agents>=0.1.0      # Bedrock drafting (LLM layer)
boto3>=1.34                # AWS SDK (DynamoDB review table, SFN callbacks)
# Deterministic core (core.py) and lambdas have no third-party deps.
